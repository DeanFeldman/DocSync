const codePattern = /^[A-Za-z0-9._~-]+$/;
const headers = { "content-type": "text/html; charset=utf-8", "content-security-policy": "default-src 'none'; script-src 'unsafe-inline'; style-src 'unsafe-inline'; base-uri 'none'; form-action 'none'; frame-ancestors 'none'", "referrer-policy": "no-referrer", "x-content-type-options": "nosniff" };
Deno.serve((request) => { const url = new URL(request.url); const code = url.searchParams.get("code"); const callback = code && codePattern.test(code) ? `za.co.docsync://auth/callback?code=${encodeURIComponent(code)}` : null; const body = !callback ? `<h1>Sign-in could not be completed.</h1><p>Return to DocSync and try again.</p>` : `<h1>✓ You're signed in to DocSync</h1><p>DocSync should now be open.</p><p><a id="open" href="${callback}">Open DocSync</a></p><p>You can safely close this window.</p><script>location.href=document.getElementById('open').href</script>`; return new Response(`<!doctype html><meta name="viewport" content="width=device-width"><title>DocSync sign-in</title><style>body{font:16px system-ui;max-width:36rem;margin:12vh auto;padding:2rem;color:#182235}a{display:inline-block;padding:.7rem 1rem;background:#087e75;color:white;border-radius:.5rem;text-decoration:none}</style>${body}`, { headers }); });
